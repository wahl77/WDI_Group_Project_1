%strong
  Hello my dear 
  = current_user.firstName

%p
  You currently are registered to be staying at 
  %br
  = gmaps("map_options" => {"auto_zoom" => false, "zoom" => 7 }, "markers" => { "data" => @markers })
  = #gmaps4rails(@markers)
  
  = #gmaps("markers" => {"data" => @markers})