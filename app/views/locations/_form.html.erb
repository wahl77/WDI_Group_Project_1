<%= form_for @location, remote:true do |f| %>
  <% if @location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@location.errors.count, "error") %> prohibited this location from being saved:</h2>

      <ul>
      <% @location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :range %><br />
    <% x = [1, 2, 5, 10, 30, 50].map{|y| ["#{y} miles", y]}%>
    <%= f.select :range, options_for_select(x, 30) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
